{
  "name": "scarcity-connect",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest --coverage",
    "prebuild": "rm -rf dist",
    "build": "babel src -d dist"
  },
  "devDependencies": {
    "@babel/cli": "^7.5.0",
    "@babel/core": "^7.5.4",
    "@babel/preset-env": "^7.5.4",
    "jest": "^24.8.0"
  },
  "dependencies": {
    "aws-sdk": "^2.502.0"
  }
}
